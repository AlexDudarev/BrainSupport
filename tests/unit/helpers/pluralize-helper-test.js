import { pluralizeHelper } from '../../../helpers/pluralize-helper';
import { module, test } from 'qunit';

module('Unit | Helper | pluralize helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluralizeHelper(42);
  assert.ok(result);
});
